# Core (required)
numpy>=1.24

# Optional extras -- the package degrades gracefully without these.
# Tabular reports / scenario comparison tables
pandas>=2.0
# Dashboard charts (ui/dashboard.py plot_* functions)
matplotlib>=3.7
# Better-fit quality-prediction / predictive-maintenance models (ml/ml_engine.py
# always has a numpy-only fallback, this upgrades it when available)
scikit-learn>=1.3
# Faster/exact solves in optimisation/ (falls back to a from-scratch search otherwise)
scipy>=1.10
# HTTP API surface (api/app.py)
fastapi>=0.110
uvicorn>=0.29
# Tests
pytest>=7.4
